# CDS Goal 工作流

CDS Goal 用于跨阶段、跨对话框、需要证据验收的长期任务。它不是普通待办清单，而是一份可持续执行、可恢复、可审计的事实合同。

## 何时使用

满足任一条件时，应建立正式 Goal：

- 工作跨越多个可独立验收阶段；
- Profile owner 明确说“开 Goal”“持续推进”“不要让我一直盯着”；
- 涉及多个仓库、PR、数据迁移、真实AI或高风险用户数据；
- 中途反馈很多，但不能丢失主线；
- 完成声明需要多类证据，而不是一个命令即可证明。

单文件、单命令或短修复不必写大型 Goal，但仍需明确事实、验收、证据和完成条件。

## 权威结构

正式 Goal 至少包含：

1. **唯一权威入口**：旧计划只作背景，当前目标、状态、门禁和证据以一份文档为准。
2. **目标**：说明最终真实能力，同时写清“不是只做什么表面结果”。
3. **当前事实**：列出已实现、未实现、未知、事实源和过期事实。
4. **范围与非范围**：防止普通反馈把 Goal 无限膨胀。
5. **硬规则**：安全、数据、版权、用户隔离或产品边界。
6. **依赖阶段**：按前置条件排序；门禁未过不得跳阶段。
7. **小节点**：每个阶段拆成可独立验收、可留下直接证据的节点。
8. **证据栏**：测试、命令、文件、URL、截图或真实路径，不写“感觉完成”。
9. **交付边界**：阶段对应哪个提交或PR，避免把整个路线图塞进一个不可审查改动。
10. **反馈分类**：当前阻塞、当前阶段修正、下一阶段输入、后续 backlog 分开处理。
11. **停止条件**：高风险错误、证据矛盾、权限或远端状态不一致时停止自动推进。
12. **Completion Audit**：逐条把显式要求映射到同范围证据。
13. **开 Goal 目标文本**：新对话框可以直接恢复同一主线。

## 执行规则

- Codex 可见 Goal 是当前执行视图；项目 `.xds/goals/` 是跨对话框持久状态。
- 同时只能有一个任务 running。
- 进度只能由 verified 任务计算，不能手填百分比。
- 每项 verify 必须附直接证据。
- 普通视觉意见和新想法不会自动取消 Goal；先按反馈分类处理。
- 答案错误、数据丢失、越权、安全风险或事实源冲突属于停止条件，不能为了连续推进而忽略。
- 阶段完成时更新权威 Goal、项目状态、任务看板和适用的错误复盘。

## 系统命令

```bash
xixi-dev-system goal lint --document doc/goals/example.md --spec doc/goals/example.goal.json
xixi-dev-system goal create --project . --spec doc/goals/example.goal.json
xixi-dev-system goal task start --project . --goal example --task T1
xixi-dev-system goal task verify --project . --goal example --task T1 \
  --evidence-type test --evidence "12 tests passed"
```

`goal lint` 是结构门禁，不证明业务目标已经完成；业务完成仍需对应阶段的真实证据。

## 完成审计问题

标记完成前逐项回答：

```text
每条用户显式要求在哪里？
每条要求由什么同范围证据证明？
是否有旧状态、测试范围或静态Demo与完成声明矛盾？
失败、权限、刷新、隔离、移动端等适用路径是否验证？
未完成项是否被隐藏在“后续优化”里？
交付是否已经按约定提交、推送或建立PR？
```

任一答案不确定，都不能把 Goal 标记 complete。
