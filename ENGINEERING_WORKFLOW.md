# 工程化工作流

## 核心目标

让项目靠文件和命令自解释，而不是靠某个聊天窗口记忆。

每个长期项目都应该具备：

- 项目入口：`AGENTS.md`
- 当前状态：`CURRENT_STATUS.md`
- 任务看板：`TASKS.md`
- 错误复盘：`错误复盘.md`
- 环境说明：`doc/ENVIRONMENT.md`
- 只读自检命令：`doctor`

## 开始任务

1. 读取全局 Agent Profile。
2. 读取项目 `AGENTS.md`、`CURRENT_STATUS.md`、`TASKS.md`。
3. 运行项目 doctor 命令。
4. 查看 Git 状态和分支。
5. 确认当前任务和完成标准。

## 完成任务

只有同时满足下面条件，才可以说完成：

- 目标行为真实实现；
- 从真实入口验证过；
- 适用的测试/检查通过；
- 用户数据、权限、错误路径按任务风险验证；
- 当前状态和下一步已写入项目记忆；
- 未完成项明确披露。

## 环境策略

- 不把 Codex 内置环境等同于用户本机环境。
- 每个项目都要能告诉用户需要什么 Node/Python/数据库。
- 环境失败时区分版本、依赖、端口、数据库、权限和代码错误。
- 不为了绕过环境问题删除核心产品逻辑。

## Git 策略

- 不直接改 `main`。
- 不强推、不硬重置。
- 提交前检查 diff。
- 如果工作区有既有改动，默认视为用户或前序 Agent 成果。
- 提交/推送/PR 需要用户明确授权，除非项目规则另有明确要求且用户已同意。

