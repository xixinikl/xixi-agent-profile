# Agent 协作操作系统

本文件把个人 Agent Profile、项目记忆和多对话框协作整合成一套固定操作系统。

## 三层结构

| 层级 | 作用 | 事实源 |
|---|---|---|
| 个人层 | 记住用户长期偏好、审美、工程习惯 | `xixi-agent-profile` |
| 项目层 | 记住项目规则、环境、任务、当前状态 | 具体项目仓库 |
| 对话层 | 处理当前这一次任务 | 当前聊天和工具输出 |

优先级：

1. 用户当前明确指令；
2. 当前项目规则和状态；
3. 个人 Agent Profile；
4. 旧聊天摘要。

## 每次开始的固定流程

Agent 必须按以下顺序启动：

1. 读取个人 Profile 的 `START_HERE.md`。
2. 读取个人 Profile 的 `AGENTS.md`、`USER_PREFERENCES.md`、`ENGINEERING_WORKFLOW.md`。
3. 读取当前项目的 `AGENTS.md`、`CURRENT_STATUS.md`、`TASKS.md`。
4. 运行项目自检命令，例如 `npm run doctor`。
5. 查看 Git 分支、远端和未提交改动。
6. 明确当前任务、范围、验收方式。

如果项目还没有记忆骨架，先按 `templates/project-memory/` 建立骨架，再开始功能开发。

## 每次结束的固定流程

Agent 结束前必须完成：

1. 说明结果：完成、部分完成或阻塞。
2. 列出实际修改文件。
3. 列出实际验证命令和结果。
4. 写清未验证项和风险。
5. 给出下一步。
6. 按 `MEMORY_UPDATE_PROTOCOL.md` 判断是否需要更新长期记忆。
7. 如果更新了个人 Profile 或项目记忆，提交并推送，或说明没有权限。

## 质量与成长闭环

每个接入质量工厂的项目按下面的闭环运行：

1. 每日验收先由脚本收集范围、检查结果和风险，再由 Agent 只读取压缩摘要。
2. `pass` 且无新风险时，只保留验收报告；`conditional`、`fail`、自动修复或未覆盖项出现时，写入项目复盘候选。
3. 周检汇总候选，确认是否重复、是否有明确预防动作、是否可跨项目复用。
4. 只有满足“重复或高影响 + 可验证规则 + 明确证据”的条目，才能进入个人层 `LEARNINGS.md`。
5. 新项目接手时读取相关项目复盘和 `LEARNINGS.md`，把已验证经验变成开发前约束或 doctor 检查。

详细格式和命令见 `GROWTH_LOOP.md`。质量报告本身面向用户；复盘候选面向后续 Agent；全局经验面向所有项目。

## 多对话框并行规则

多个对话框可以同时工作，但必须避免互相覆盖：

- 每个对话框只负责一个明确任务。
- 每个长期任务都要在项目 `TASKS.md` 中登记。
- 每个对话框开始前先看 `CURRENT_STATUS.md` 和 Git 分支。
- 不确定是否冲突时，先只读检查，不修改文件。
- 交接时必须留下“当前分支、已改文件、已验证、未验证、下一步”。

## 什么写进个人 Profile

写入个人 Profile 的内容必须跨项目长期有效，例如：

- 用户偏好的解释方式；
- UI 审美；
- 工程工作法；
- 反复出现的环境坑；
- 多电脑、多 Codex 使用方式；
- 新项目启动模板。

不要把某个项目的具体功能进度写进个人 Profile。

## 什么写进项目仓库

写入项目仓库的内容包括：

- 项目架构和启动命令；
- 当前任务状态；
- 项目规则；
- 环境版本；
- 数据库和依赖说明；
- 项目内错误复盘；
- 本项目的验收证据。

## 用户最小负担

用户只需要记一句：

```text
先读我的 Agent Profile，再处理当前项目。
```

如果 Agent 不知道 Profile 地址，给它：

```text
https://github.com/xixinikl/xixi-agent-profile
```
