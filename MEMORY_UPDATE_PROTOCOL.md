# 记忆更新协议

个人 Agent Profile 不会自动读心更新。它依赖 Agent 在合适时机把长期有效的信息写回文件。

本协议定义：什么时候更新、写到哪里、如何避免越写越乱。

## 什么时候必须更新

出现以下情况时，Agent 必须考虑更新记忆：

- 用户明确说“记住这个”“以后都这样”“我习惯是”“我不喜欢”；
- 用户纠正了非显而易见的错误；
- 某个环境坑会跨电脑或跨项目复现；
- 某个工程流程被反复使用；
- 某个 UI/产品偏好对多个项目都有效；
- 新建了长期项目或长期协作规则；
- 发现旧记忆不准确、过期或互相冲突。

## 写到哪里

| 信息类型 | 文件 |
|---|---|
| 沟通、协作、解释偏好 | `USER_PREFERENCES.md` |
| UI、审美、交互偏好 | `UI_PREFERENCES.md` |
| 工程流程、验证、Git、环境策略 | `ENGINEERING_WORKFLOW.md` |
| 跨电脑、全新 Codex、迁移方式 | `PORTABILITY.md` |
| Agent 启动和全局硬规则 | `AGENTS.md` |
| 具体项目当前状态 | 项目仓库的 `CURRENT_STATUS.md` |
| 具体项目任务和验收 | 项目仓库的 `TASKS.md` |
| 具体项目错误复盘 | 项目仓库的 `错误复盘.md` |

个人 Profile 只记录跨项目长期有效的偏好和方法。具体项目事实不要塞进个人 Profile。

## 更新前检查

更新前先判断：

1. 这是长期有效，还是只适用于当前任务？
2. 这是个人偏好，还是项目事实？
3. 是否已有类似规则，可以合并而不是重复新增？
4. 是否需要标注日期或背景，避免以后误用？

不确定时，先写到当前项目 `CURRENT_STATUS.md` 的已知边界，不要急着写入全局 Profile。

## 更新方式

建议流程：

```bash
cd /path/to/xixi-agent-profile
git pull --ff-only
# 修改对应文件
git diff
git add <files>
git commit -m "docs: update agent memory"
git push
```

如果当前 Agent 没有 GitHub 写权限，应在最终回复中给出建议更新内容，用户可交给有权限的 Agent 写入。

## 防止记忆污染

禁止写入：

- 一次性情绪、临时想法、未确认猜测；
- 密码、Token、Cookie、真实隐私数据；
- 某个项目的短期任务细节；
- 与已有规则冲突但没有说明取舍的新规则；
- “下次注意”这种不可验证空话。

## 验收报告的回流规则

每日验收不是长期记忆本身。按以下层级保存：

| 结果 | 落点 | 是否自动写入 |
|---|---|---|
| `pass` 且无异常 | 质量台/验收报告 | 是 |
| `conditional`、`fail`、自动修复、未覆盖项 | 项目 `doc/retrospectives/` 候选 | 是 |
| 重复或高影响的可复用规律 | `LEARNINGS.md` | 仅周检确认后 |

使用 `scripts/ingest-acceptance-report.sh` 创建项目候选。候选必须保留报告来源、验收日期、事实、影响、预防动作和升级状态。禁止依据单次失败自动改写全局偏好或工程规则。

## 每次任务结束时的记忆检查

长期项目任务结束前，Agent 问自己：

```text
这次有没有学到跨项目也适用的用户偏好或工程规则？
这次有没有用户纠正过我，且以后应该防复发？
如果有，是否已经写入个人 Profile 或项目复盘？
```

没有需要更新时，明确说“本次没有新增长期记忆”。
