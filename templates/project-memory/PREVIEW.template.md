# {{PROJECT_NAME}} 预览说明

本文件记录当前项目怎么预览。用户不应该记命令；Agent 每次改动可见功能后必须更新这里或 `preview.config.json`。

## 当前预览状态

| 入口 | 状态 | 地址或命令 | 说明 |
|---|---|---|---|
| {{PREVIEW_NAME}} | {{STATUS}} | `{{URL_OR_COMMAND}}` | {{NOTES}} |

## 启动方式

```bash
{{START_COMMAND}}
```

## 停止方式

```bash
{{STOP_COMMAND}}
```

## 验证方式

```bash
{{HEALTH_COMMAND}}
```

## 已知边界

- {{KNOWN_LIMIT}}
