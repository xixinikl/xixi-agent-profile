# 记忆维护节奏

长期记忆需要定期整理，否则会变成新的混乱源。

## 每次任务结束

检查：

- 是否有新的长期偏好；
- 是否有用户纠正过的错误；
- 是否有跨项目环境坑；
- 是否有项目状态需要更新；
- 是否有旧规则失效。

有则按 `MEMORY_UPDATE_PROTOCOL.md` 更新；没有则说明“本次没有新增长期记忆”。

## 每周或每个阶段结束

建议做一次轻量整理：

1. 读取 `PROJECTS.md`，确认活跃项目状态。
2. 检查个人 Profile 是否有重复、冲突或过期规则。
3. 检查活跃项目的 `CURRENT_STATUS.md` 是否仍准确。
4. 检查活跃项目是否能运行 doctor 自检。
5. 把已归档项目标记为 `暂停` 或 `归档`。

## 何时重构记忆

出现以下情况时再整理结构，不要频繁重构：

- 同一规则出现在多个文件且表述冲突；
- 新项目越来越多，项目索引难以扫描；
- 用户偏好发生明显变化；
- 某个流程反复造成误解。

## 禁止事项

- 不为了“整洁”删除历史复盘。
- 不把具体项目进度塞进个人 Profile。
- 不把临时情绪写成长期规则。
- 不在没有用户确认时改写用户核心偏好。

